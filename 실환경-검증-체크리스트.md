# 현장 앱 — 실환경에서만 확인되는 것 (2026-07-30)

자동 테스트는 **가짜 서버와 가짜 브라우저** 위에서 돕니다.
아래 항목은 그 안에서 확인할 방법이 없어 남겨 둡니다.
사람이 눌러 보고 ✅ 를 채워야 "됐다"고 말할 수 있습니다.

---

## A. Apps Script 계약 서버가 붙어야 확인되는 것

앞선 단계: manmool 저장소 `apps-script-contract/` 를 배포하고 `/exec` 주소를 받는 것.

| | 확인할 것 | 어떻게 |
|---|---|---|
| ☐ A-1 | ☁️ 설정에 `/exec` 주소·`ADMIN_TOKEN` 저장이 되는가 | 저장 후 상태가 `🔒 자가진단 필요` 로 바뀌는지 |
| ☐ A-2 | **[🩺 자가진단]이 통과하는가** | 항목별 ✅/❌ 가 나오고, 전부 ✅ 여야 열립니다 |
| ☐ A-3 | 자가진단 통과 후 `📨 알림톡 계약서` 버튼이 눌리는가 | 통과 전에는 눌리지 않아야 정상입니다 |
| ☐ A-4 | 계약 생성이 실제로 되는가 | 스프레드시트 `Contracts` 에 한 줄이 생기는지 |
| ☐ A-5 | 서명 링크가 폰에서 열리는가 | 링크를 사장님 폰에 보내 직접 열어 보기 |
| ☐ A-6 | 폰에서 **손가락으로 서명이 그려지는가** | 캔버스 터치는 실기기에서만 확인됩니다 |
| ☐ A-7 | 서명 후 완료 PDF 가 Drive 에 생기는가 | `전자계약/<계약번호> <고객명>/완료_*.pdf` |
| ☐ A-8 | 완료본 PDF 안에 **서명 이미지가 실제로 보이는가** | 구글 HTML→PDF 변환이 이미지를 못 그리는 경우가 드물게 있습니다 |
| ☐ A-9 | `✍️ 서명 확인` 이 COMPLETED 를 받아오는가 | 현장 앱 프로젝트 창에서 |
| ☐ A-10 | 잘못된 토큰·기한 지난 링크가 각각 다른 안내를 보이는가 | 링크 뒤 글자를 바꿔 열어 보기 |

## A-2. AI 키를 서버로 옮긴 뒤 (무료 Gemini)

앞선 단계: 계약 서버 스크립트 속성에 `GEMINI_API_KEY` 를 넣고 **새 버전으로 다시 배포**.
(배포를 다시 하지 않으면 속성을 넣어도 옛 버전이 계속 돕니다.)

| | 확인할 것 | 어떻게 |
|---|---|---|
| ☐ AI-1 | `/exec?action=health` 응답에 `"ai":{"gemini":true}` 가 오는가 | 주소창에서 바로 |
| ☐ AI-2 | 앱을 껐다 켠 뒤 ⚙️ 설정에 **"AI 키는 계약 서버에 있습니다"** 가 뜨는가 | 부팅할 때 한 번만 물어봅니다 |
| ☐ AI-3 | **기기 Gemini 키를 [지우기] 한 뒤에도** AI 가 답하는가 | 이것이 이번 작업의 목적입니다 |
| ☐ AI-4 | 폰과 PC **둘 다** 기기 키 없이 되는가 | 기기마다 넣는 일을 없애는 것이 목적 |
| ☐ AI-5 | 하루 200건 근처에서 `AI 한도` 안내가 뜨는가 | 서버 한 곳에서 셉니다 |
| ☐ AI-6 | 서버 속성에서 키를 빼면 기기 키로 되돌아가는가 | 되돌아가야 정상(AI 가 멈추면 안 됩니다) |

> ⚠️ **AI-0 (제일 먼저)** — aistudio 키가 속한 구글 프로젝트에 **결제계정이 연결돼 있지 않은지** 확인하세요.
> console.cloud.google.com → 결제. 연결돼 있으면 Gemini 는 무료가 아니라 **쓴 만큼 청구**됩니다.
> 결제 관련 메일을 받으신 적이 있다면 연결된 것입니다. 결제 없는 새 프로젝트에서 키를 다시 발급하세요.
>
> ChatGPT(`OPENAI_API_KEY`)는 **쓰지 않기로 했습니다.** 서버가 `AI_PAID_BLOCKED` 로 막습니다.
> 정말 쓰시려면 스크립트 속성 `AI_ALLOW_PAID=1` 이 필요합니다 — 요금이 붙는다는 뜻입니다.

## B. 발송 — 켜기 전에는 확인할 수 없는 것

**⚠️ 아래는 사장님이 명시적으로 켠 뒤에만 하십시오. 지금은 전부 꺼져 있습니다.**

| | 확인할 것 | 주의 |
|---|---|---|
| ☐ B-1 | 솔라피 발신번호 등록 완료 | 등록 전에는 아무것도 안 나갑니다 |
| ☐ B-2 | **본인 번호로 테스트 발송이 도착하는가** | 고객 번호로 먼저 시험하지 마십시오 |
| ☐ B-3 | 도착 확인 후에만 `ALIMTALK_LIVE=1` | 이 순서를 바꾸면 고객에게 시험 문자가 갑니다 |
| ☐ B-4 | 켠 뒤 `notify.sent` 가 정말 true 로 오는가 | false 면 앱이 "보냈다"고 말하지 않는 것이 정상 |

## C. 실기기에서만 확인되는 화면

자동 테스트는 크로미움 가상 화면을 씁니다. 실제 폰의 브라우저·인앱뷰는 다릅니다.

| | 확인할 것 | 어디서 |
|---|---|---|
| ☐ C-1 | 카톡 인앱 브라우저에서 서명 화면이 하얗게 뜨지 않는가 | 카톡으로 링크 보내 열기 |
| ☐ C-2 | 아이폰 사파리에서 모달이 열렸을 때 뒤 화면이 안 밀리는가 | iOS 는 `overflow:hidden` 이 완전하지 않습니다 |
| ☐ C-3 | 폰 키보드가 올라올 때 입력칸이 가려지지 않는가 | 금액·성명 입력 |
| ☐ C-4 | 사진 촬영 → 업로드가 폰에서 되는가 | 갤러리·카메라 권한 |
| ☐ C-5 | 홈화면에 추가한 PWA 에서 새 버전(v172-aptstats)이 잡히는가 | 앱을 완전히 닫고 다시 열기 |
| ☐ C-6 | 화면읽기(보이스오버·톡백)로 탭 이동이 되는가 | 자동 테스트는 ARIA 속성만 봅니다 |

## D. 배포 후

| | 확인할 것 |
|---|---|
| ☐ D-1 | GitHub Pages 배포가 성공했는가 (Actions 초록불) |
| ☐ D-2 | 배포된 화면의 `APP_BUILD` 가 `hyeonjang-v231-officeintakefix` 인가 |
| ☐ D-3 | 기존 현장·사진·견적 자료가 그대로 보이는가 (드라이브에서 불러오기) |

---

## 자동 테스트가 이미 지키고 있는 것 (다시 확인 안 하셔도 됩니다)

- 종료된 `manmool-contract.fly.dev` 주소가 소스에 없음
- 자가진단 통과 전에는 계약 네트워크 요청 0건 · 발송 이력 0건
- 자가진단 실패 시 잠긴 상태 유지 / 주소 변경 시 예전 통과 기록 무효화
- `notify.sent !== true` 면 "보냈다"로 기록하지 않음
- 관리자 토큰이 직렬화(클라우드 백업)·화면 소스에 남지 않음
- 고객 1회용 서명 토큰을 앱이 저장하지 않음
- 키보드 탭 이동 · 모달 Esc · 초점 복귀 · 초점 갇힘
- 아이콘 버튼 이름 · 금액칸 라벨·숫자 키패드
- PC(1280)·폰(390) 가로 넘침 없음 · 폰 터치 크기 40px
