# 현장 웹 검색 개선 검토 — v262 후보

작성일: 2026-09-06

## 요약

- 운영 공개 버전: `hyeonjang-v261-orderflow` (공개 `sw.js` 및 브라우저 화면 확인).
- 개발 후보: `hyeonjang-v262-searchrecovery`.
- 현재 범위: 사진 검색 0건 복귀, 진행·보관 현장 통합 검색, Windows 오프라인 사진 점검 도구 실행 호환성.
- 작업 브랜치: `codex/field-ops-usability-20260906` (시작 커밋 `3e5604c`).
- 실제 사진·프로젝트·고객 기록·서버 설정은 변경하지 않음. 테스트는 가상 자료와 격리된 브라우저에서 수행.
- 운영 배포는 별도 승인 대상. 기존 미완료 작업 폴더는 보존하고 최신 main의 별도 worktree에서 개발.

## 확인한 문제와 수정

| 우선순위 | 재현 및 근거 | 수정 |
|---|---|---|
| P2 | 사진을 보유해도 `viewPhotos()`에서 검색 결과가 0건이면 신규 사진 가져오기 안내가 표시됨 | 보유 사진 0건과 검색 일치 0건을 분리. ‘검색 지우고 사진 보기’로 입력창·검색 조건을 함께 초기화 |
| P2 | `openProjectSheet()`는 접힌 보관 현장 행을 DOM에 만들지 않아 검색에서 제외됨 | 전체 현장 목록을 기준으로 검색. 공백·대소문자 차이 허용, 진행/보관 결과 수·검색 없음 안내·지우기 제공 |
| P2 | Windows에서 오프라인 사진 점검 도구의 모듈 로딩 및 CLI 진입 URL 비교가 실패함 | 테스트의 동적 import와 도구 진입 경로를 `pathToFileURL`로 변환. 사진 판정·백업·수정 규칙은 그대로 유지 |

## 보존 사항

- 검색 지우기는 원본 파일·현장 배정·선택 ID·선택 모드를 변경하지 않음.
- 보관 현장은 검색·열기만 가능하며 자동으로 보관 해제하지 않음.
- 검색 중에만 보관 결과를 표시하고, 검색 지우기 후 원래 보관 목록의 접힘 상태 복원.
- 사진이 실제로 없을 때의 기존 가져오기 안내 유지.
- 정상 사진 목록에는 새 상단 줄을 추가하지 않아 기존 첫 사진 위치 유지.
- 데이터 스키마·직렬화 키·Google Drive·Apps Script·권한·고객 발송 설정 변경 없음.

## 검증 현황

- 스크립트 문법 및 화면/서비스워커 버전 일치: 통과.
- 기존 모바일 목록 검사: 9/9 통과. 미일치 항목이 DOM에 없을 때도 ‘안 보임’으로 판정하도록 테스트를 보완함.
- 신규 화면 검사: 390px/1280px 총 8개 시나리오 통과, JavaScript 오류 0건. 검색 복구·보관 검색·키보드·44px 조작부·가로 넘침·직렬화 데이터 보존·검색 중 `markDirty` 호출 0 확인.
- 변이 검사: `photo-clear`는 검색 초기화가 작동하지 않을 때 실패(exit 1, 변이 1회 적용). `project-archived`는 보관 결과를 숨길 때 실패(exit 1, 변이 1회 적용). 격리 브라우저만 변경하고 운영 소스·자료는 변경하지 않음.
- Windows 사진 점검 도구: 가상 임시 자료로 CLI 보고서·백업·수정본 생성과 원본 불변 검증 통과. 실제 현장 데이터 파일에는 실행하지 않음.
- 최종 전체 회귀: `node tests/run-all.js`, **117개 중 117개 통과, 0개 실패, 종료 코드 0**, 13.3분, 병렬 3개.
- 재실행 이력: `office-ops-conversion.e2e.js`는 병렬 실행 중 단언 실패 후 단독 재실행 통과(104.4초). `office-ops-isolation.e2e.js`는 병렬 실행 180초 제한 후 단독 재실행 통과(144.9초). 러너의 기존 재시도 1회 정책을 사용했으며 제한·검사를 완화하지 않음. 같은 현상이 반복되면 해당 테스트의 경합/실행 부하를 별도 점검.
- 검증 범위: 자동 브라우저와 모의 서버. 실제 iPhone/Android 터치·키보드 및 운영 계정 연동은 별도 실기기 확인 대상.

## 후속 후보 / 확인 필요

1. 통합 검색 결과를 누르면 해당 탭만 열리는 흐름을 선택한 사진·일지·현장으로 직접 연결.
2. 같은 날 일정이 여러 건일 때 음성 작업일지를 붙일 대상 일정을 저장 전에 명시적으로 선택.
3. 승인된 배포 이후 공개 버전 일치 및 실제 휴대전화 키보드·터치 확인.

후속 후보는 이번 변경에 포함하지 않으며, 운영 자료의 수정·삭제·자동 전송도 수행하지 않음.
