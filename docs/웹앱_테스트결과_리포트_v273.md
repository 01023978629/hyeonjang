# v273 현장 모바일 관리사무소 연결 화면

2026-09-08 · `hyeonjang-v273-officeconnect` · 로컬 후보

## 추가 기능

- 더보기 → 웹 업무 연결 센터 상단에 관리사무소 상태판.
- 접수 검토 대기, 24시간 미확인, 서버 전송 대기, 전송 차단을 별도로 표시.
- 마지막 **성공** 조회만 표시하며 잘못된/미래 시각은 성공 기록으로 인정하지 않는다.
- 연결 미설정, 오프라인, 오류 기록, 미조회, 15분 이상 오래된 조회를 구분.
- 기존 타이머에서 요약 영역만 갱신하고 입력·포커스·선택 범위를 보존.
- 기존 직원 접수 검토·아파트 오더·연결 설정과 직원 포털 로그인 바로가기.
- 명시적 갱신 버튼의 연타 방지와 닫힌 화면/새 화면에 늦은 응답 반영 차단.

실시간 서버 정상 또는 전송 완료를 단정하지 않는다. 새 타이머/API·자동 접수·
자동 재전송·사진 배정·프로젝트 변경·저장 스키마 추가는 없다. 112개 더보기 메뉴를 유지한다.
v272의 서류 메타데이터 줄바꿈 수정과 버전 표기를 통합했다.

## 검증 기록

- 기능 소스 최종 개별 검사 14/14 및 기존 `web-work-bridge` 원본 검사 통과.
- 320/390/1280px, 큰 글씨 2단계, 44px 조작 영역, 키보드·스크롤 접근 확인.
- 가상 상태의 원문 직렬화 불변, 개인정보·원문 오류 비노출, 실제 기존 진입 함수 확인.
- 변이 `freshness`/`readonly`/`late` 각각 exit 1로 잘못된 성공 시각·자료 변경·
  새 화면에 과거 오류 반영을 검출했다. 기존 단언과 시간 제한을 유지한다.
- v272 통합 및 v273 버전 변경 후 최종 전체 회귀는 127개다. 결과는 완료 후 기록한다.

개별 로그/캡처: 작업 루트의 `tmp/mobile-office-connect-20260908/final/`.
실제 데이터 대신 가상 자료와 별도 브라우저를 사용했다.
운영 계정 로그인·실제 단지 매핑·실제 휴대폰·서버 접수/전송은 별도 확인 대상이다.

## 배포 경계

이 후보는 새 기능 공개 배포 승인 전이다. v272의 기존 개선 배포 수정과 분리한다.
Apps Script·Google 계정·마스터 계정·역할·비밀번호는 변경하지 않았다.
